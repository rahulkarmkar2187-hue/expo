import React, { useState } from "react"; import { Button } from "@/components/ui/button"; import { Card, CardContent } from "@/components/ui/card";

export default function App() { const [video, setVideo] = useState(null); const [loading, setLoading] = useState(false); const [output, setOutput] = useState(null);

const handleUpload = (e) => { const file = e.target.files[0]; setVideo(file); };

const convertVideo = async () => { if (!video) return; setLoading(true);

// Fake AI conversion (placeholder)
setTimeout(() => {
  setOutput("converted_3d_cartoon_video.mp4");
  setLoading(false);
}, 3000);

};

return ( <div className="min-h-screen bg-gray-100 flex items-center justify-center p-6"> <Card className="w-full max-w-lg shadow-xl rounded-2xl"> <CardContent className="p-6 space-y-6"> <h1 className="text-2xl font-bold text-center">3D Cartoon Video Converter</h1>

<input
        type="file"
        accept="video/*"
        onChange={handleUpload}
        className="w-full border p-2 rounded-xl"
      />

      <Button
        onClick={convertVideo}
        disabled={loading}
        className="w-full rounded-2xl p-3 text-lg"
      >
        {loading ? "Converting..." : "Convert to 3D Cartoon"}
      </Button>

      {output && (
        <div className="text-center p-4 bg-white rounded-xl shadow">
          <p className="font-semibold">Conversion Complete!</p>
          <a
            href={output}
            download
            className="text-blue-600 underline"
          >
            Download Cartoon Video
          </a>
        </div>
      )}
    </CardContent>
  </Card>
</div>

); }
